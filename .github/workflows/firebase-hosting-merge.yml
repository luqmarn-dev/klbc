# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - run: |
          sed -i 's/\${{ secrets.FIREBASE_apiKey }}/${{ secrets.FIREBASE_apiKey }}/' ./src/environments/environment.prod.ts
          sed -i 's/\${{ secrets.FIREBASE_authDomain }}/${{ secrets.FIREBASE_authDomain }}/' ./src/environments/environment.prod.ts
          sed -i 's/\${{ secrets.FIREBASE_projectId }}/${{ secrets.FIREBASE_projectId }}/' ./src/environments/environment.prod.ts
          sed -i 's/\${{ secrets.FIREBASE_storageBucket }}/${{ secrets.FIREBASE_storageBucket }}/' ./src/environments/environment.prod.ts
          sed -i 's/\${{ secrets.FIREBASE_messagingSenderId }}/${{ secrets.FIREBASE_messagingSenderId }}/' ./src/environments/environment.prod.ts
          sed -i 's/\${{ secrets.FIREBASE_appId }}/${{ secrets.FIREBASE_appId }}/' ./src/environments/environment.prod.ts
          sed -i 's/\${{ secrets.FIREBASE_measurementId }}/${{ secrets.FIREBASE_measurementId }}/' ./src/environments/environment.prod.ts
          sed -i 's/\${{ secrets.FIREBASE_databaseURL }}/${{ secrets.FIREBASE_databaseURL }}/' ./src/environments/environment.prod.ts
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KLBC_938A9 }}
          channelId: live
          projectId: klbc-938a9
