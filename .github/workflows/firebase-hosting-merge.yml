# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: franzbischoff/replace_envs@v2
        with:
          from_file: environment.prod.ts
          to_file: environment.prod.ts
        env:
          secrets.FIREBASE_apiKey: ${{ secrets.FIREBASE_apiKey }}
          secrets.FIREBASE_authDomain: ${{ secrets.FIREBASE_authDomain }}
          secrets.FIREBASE_projectId: ${{ secrets.FIREBASE_projectId }}
          secrets.FIREBASE_storageBucket: ${{ secrets.FIREBASE_storageBucket }}
          secrets.FIREBASE_messagingSenderId: ${{ secrets.FIREBASE_messagingSenderId }}
          secrets.FIREBASE_appId: ${{ secrets.FIREBASE_appId }}
          secrets.FIREBASE_measurementId: ${{ secrets.FIREBASE_measurementId }}
          secrets.FIREBASE_databaseURL: ${{ secrets.FIREBASE_databaseURL }}
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KLBC_938A9 }}
          channelId: live
          projectId: klbc-938a9
